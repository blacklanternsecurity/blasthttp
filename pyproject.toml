[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "blasthttp"
version = "0.1.0"
description = "Offensive-first HTTP library"
requires-python = ">=3.9"

[tool.maturin]
features = ["python"]
